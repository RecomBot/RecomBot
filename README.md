# RecomBot - Персональный гид по местам отдыха

## О проекте

**Travel Guide Bot** — это диалоговая система для персонализированных рекомендаций мест отдыха и развлечений. Проект объединяет:

- 🤖 **Telegram-бота** с интуитивным интерфейсом
- 🧠 **LLM-интеллект** (Ollama Cloud) для анализа запросов
- 🗺️ **Парсинг данных** из Яндекс.Афиши и Яндекс.Карт
- ⭐ **Рейтинговую систему** с отзывами пользователей
- 👥 **Ролевую модель** (user/moderator/admin)
- 🔍 **Интеллектуальный поиск** с пониманием естественного языка

## ✨ Ключевые возможности

### 🤖 **Умный Telegram-бот**
- Естественный диалог с пользователем
- Кнопочный интерфейс и inline-навигация
- Пагинация и сортировка результатов
- Оставление и модерация отзывов
- Персональные рекомендации

### 🧠 **Интеллектуальный поиск**
- **Обычный поиск** — быстрый поиск по базе данных
- **🤖 ИИ-поиск** — анализ запроса LLM и подбор релевантных мест
- **⭐ Популярные места** — топ мест по рейтингу и отзывам
- **🔍 По категориям** — фильтрация по типам заведений

### 📊 **База данных мест**
- 10+ категорий (кафе, рестораны, парки, музеи, кино и др.)
- Автоматический парсинг из Яндекс.Афиши и Яндекс.Карт
- Актуальная информация: адреса, время работы, цены
- Рейтинги на основе пользовательских отзывов

### ⭐ **Система отзывов**
- 5-звездочная система оценок
- Автоматическая модерация через LLM
- Суммаризация отзывов ИИ
- Обновление рейтингов в реальном времени

### Технологический стек

| Компонент | Технологии | Назначение |
|-----------|------------|------------|
| **Backend** | FastAPI, SQLAlchemy, Pydantic | REST API, бизнес-логика, модели данных |
| **База данных** | PostgreSQL, AsyncPG | Хранение пользователей, мест, отзывов |
| **Кэширование** | Redis | Кэширование популярных запросов |
| **LLM** | Ollama Cloud (Qwen3-coder) | Анализ отзывов, рекомендации, суммаризация |
| **Telegram Bot** | aiogram 3.x | Пользовательский интерфейс, диалоги |
| **Парсинг** | Selenium, BeautifulSoup | Сбор данных с внешних источников |
| **Контейнеризация** | Docker, Docker Compose | Оркестрация сервисов |
| **Очереди** | (планируется) Celery | Фоновые задачи, парсинг |

## 🚀 Быстрый старт

### Предварительные требования

- Docker и Docker Compose
- Telegram Bot Token ([@BotFather](https://t.me/botfather))
- Ollama Cloud API Key (опционально)

### Установка за 5 минут

1. **Клонируйте репозиторий**
```bash
git clone <repository-url>
cd RecomBot
```

2. **Настройте переменные окружения**
```bash
# Создайте файл .env в папке backend
cd backend
cp .env.example .env
# Отредактируйте .env файл
```

3. **Запустите систему**
```bash
docker-compose up -d
```

4. **Проверьте работоспособность**
```bash
# API
curl http://localhost:8001/health

# База данных
docker exec -it travel_postgres psql -U postgres -d travel_db -c "SELECT COUNT(*) FROM places;"

# Бот
# Откройте Telegram и найдите вашего бота
```

### Конфигурация (.env)
```env
# Telegram Bot
BOT_TOKEN=your_telegram_bot_token_here

# Database
DATABASE_URL=postgresql+asyncpg://postgres:postgres@postgres:5432/travel_db

# LLM (Ollama Cloud)
OLLAMA_HOST=https://ollama.com
OLLAMA_MODEL=qwen3-coder:480b-cloud
OLLAMA_API_KEY=your_ollama_api_key_here

# Redis
REDIS_URL=redis://redis:6379/0

# Parser
PARSE_INTERVAL_HOURS=24
PLACE_EXPIRY_DAYS=30
```

## 📱 Функционал Telegram-бота

### Основные команды
| Команда | Описание |
|---------|----------|
| `/start` | Начало работы, регистрация |
| `/ask` | Быстрый поиск мест |
| `/popular` | Популярные места |
| `/me` | Профиль пользователя |
| `/help` | Справка по командам |
| `/cancel` | Отмена текущего действия |

### Взаимодействие с ботом

#### 🔍 **Поиск мест**
```
Пользователь: "Хочу в уютное кафе с Wi-Fi"
Бот: 🔍 Анализирую запрос...
      🤖 Нашёл 5 подходящих мест:
      
      1. ☕ Кофейня Central Perk
         📝 Уютное место с лучшим кофе...
         ⭐ Рейтинг: 4.7/5 (23 отзыва)
         📍 ул. Центральная, 10
```

#### ⭐ **Популярные места**
- Сортировка по рейтингу или количеству отзывов
- Пагинация (вперёд/назад)
- Места в виде интерактивных кнопок
- Детальная информация при клике

#### ✍️ **Оставление отзывов**
1. Выберите место
2. Оцените от 1 до 5 звёзд
3. Напишите текст отзыва
4. LLM проверяет содержание
5. Отзыв публикуется (или отправляется на модерацию)

#### 👑 **Модерация** (для модераторов/админов)
- Просмотр очереди отзывов
- Одобрение/отклонение отзывов
- Управление местами и пользователями

## 🔧 API Endpoints

### Основные группы эндпоинтов

| Группа | Путь | Описание |
|--------|------|----------|
| **Аутентификация** | `/api/v1/auth/*` | Управление пользователями, ролями |
| **Места** | `/api/v1/places/*` | CRUD операций с местами, поиск |
| **Отзывы** | `/api/v1/reviews/*` | Создание, получение, модерация отзывов |
| **Рекомендации** | `/api/v1/recommendations/*` | Интеллектуальный поиск, рекомендации |
| **Парсер** | `/api/v1/parser/*` | Управление парсингом, статистика |
| **Модерация** | `/api/v1/moderation/*` | Модерация контента |

### Ключевые эндпоинты

```http
# Получение мест
GET /api/v1/places/?category=cafe&city=Москва&limit=10

# Интеллектуальный поиск
POST /api/v1/recommendations/intelligent-search
Content-Type: application/json
{
  "query": "романтический ресторан с видом",
  "telegram_id": 123456789
}

# Создание отзыва
POST /api/v1/reviews/
Content-Type: application/json
{
  "place_id": "uuid",
  "rating": 5,
  "text": "Отличное место!"
}
```

## 🧠 LLM Интеграция

### Использование Ollama Cloud

Система использует **Qwen3-coder:480b-cloud** для:
1. **Анализ отзывов** — проверка на уместность, токсичность
2. **Суммаризация** — создание кратких выжимок из отзывов
3. **Рекомендации** — персонализированные предложения
4. **Интеллектуальный поиск** — анализ запросов пользователей

### Пример промпта для анализа поискового запроса
```python
"""
Пользователь ищет: "уютное кафе с Wi-Fi для работы"

LLM определяет:
- Категория: cafe
- Ключевые слова: ["уютное", "wi-fi", "работа", "кофе"]
- Город: Москва (по умолчанию)
- Приоритет сортировки: rating
"""
```

## 📊 Парсинг данных

### Источники данных
| Источник | Тип данных | Частота обновления | Количество |
|----------|------------|-------------------|------------|
| **Яндекс.Афиша** | Мероприятия (концерты, театры, выставки) | Каждые 6 часов | До 10 на категорию |
| **Яндекс.Карты** | Постоянные места (кафе, рестораны, парки) | Раз в сутки | До 15 на категорию |

### Поддерживаемые категории
- ☕ Кафе и кофейни
- 🍽️ Рестораны
- 🌳 Парки и скверы
- 🏛️ Музеи и галереи
- 🎬 Кинотеатры
- 🎭 Театры
- 🍸 Бары и пабы
- 🛍️ Торговые центры
- 🎨 Выставки и искусство

## 👥 Ролевая модель

### Уровни доступа

| Роль | Возможности |
|------|-------------|
| **User** | Поиск мест, оставление отзывов, просмотр рекомендаций |
| **Moderator** | Всё что у User + модерация отзывов, управление местами |
| **Admin** | Полный доступ ко всем функциям системы |

### Автоматическая модерация
1. Пользователь оставляет отзыв
2. LLM проверяет текст на:
   - Нецензурную лексику
   - Оскорбления и агрессию
   - Спам и рекламу
3. Результат:
   - ✅ Одобрено — сразу публикуется
   - ⚠️ Флаг — на ручную модерацию
   - ❌ Отклонено — с объяснением причины

## 🗃️ Модели данных

### Основные сущности

```python
# Пользователь
class User:
    id: UUID
    telegram_id: int  # Уникальный идентификатор из Telegram
    username: str
    role: str  # user/moderator/admin
    preferences: JSON  # Предпочтения пользователя

# Место
class Place:
    id: UUID
    name: str
    category: str  # cafe, restaurant, park и т.д.
    description: str
    address: str
    rating: float  # Средний рейтинг
    tags: List[str]
    working_hours: str
    price_level: int  # 1-3

# Отзыв
class Review:
    id: UUID
    user_id: UUID
    place_id: UUID
    rating: int  # 1-5
    text: str
    llm_check: JSON  # Результат проверки LLM
    moderation_status: str  # pending/approved/flagged_by_llm/rejected
```

## 🐳 Docker контейнеры

| Сервис | Порт | Описание |
|--------|------|----------|
| **backend** | 8001:8000 | FastAPI приложение |
| **telegram-bot** | - | Telegram бот |
| **parser** | - | Сервис парсинга |
| **postgres** | 5432:5432 | PostgreSQL база данных |
| **redis** | 6379:6379 | Redis кэш |

## 📈 Мониторинг и логи

### Логирование
```bash
# Просмотр логов всех сервисов
docker-compose logs -f

# Логи конкретного сервиса
docker-compose logs -f backend
docker-compose logs -f telegram-bot
docker-compose logs -f parser

# Логи базы данных
docker-compose logs -f postgres
```

## 🚀 Производительность

### Оптимизации
- **Кэширование** — Redis для популярных запросов
- **Индексация** — оптимизированные индексы в PostgreSQL
- **Пагинация** — постраничная загрузка результатов
- **Асинхронность** — async/await для всех I/O операций
- **Пакетная обработка** — групповые операции с БД

### Масштабирование
- Горизонтальное масштабирование бэкенда
- Репликация базы данных
- Redis кластер для кэширования
- Очереди задач для фоновой обработки

### Структура проекта
```
RecomBot/
├── backend/                    # FastAPI приложение
│   ├── src/
│   │   ├── main_single.py     # Основной файл приложения
│   │   ├── config.py          # Конфигурация
│   │   ├── llm_processor.py   # LLM обработчик
│   │   ├── parser_main.py     # Главный парсер
│   │   ├── parser_unified.py  # Парсер Яндекс.Афиши
│   │   └── yandex_maps_llm_parser_full.py # Парсер Яндекс.Карт
│   ├── Dockerfile
│   ├── docker-compose.yml
│   └── requirements.txt
├── GidRecBot/                  # Telegram бот
│   ├── bot/
│   │   ├── handlers/          # Обработчики сообщений
│   │   ├── keyboards/         # Клавиатуры
│   │   ├── middlewares/       # Middleware
│   │   ├── states/            # FSM состояния
│   │   ├── utils/             # Утилиты
│   │   └── __main__.py        # Точка входа
│   ├── Dockerfile.bot
│   └── requirements.txt
└── .env                        # Переменные окружения
```



## 🎨 Дополнительные файлы для проекта

### `.env.example` файл:
```env
# ============================================
# TRAVEL GUIDE BOT - Конфигурация окружения
# ============================================

# Телеграм Бот
BOT_TOKEN=your_telegram_bot_token_here

# База данных PostgreSQL
DATABASE_URL=postgresql+asyncpg://postgres:postgres@postgres:5432/travel_db

# LLM (Ollama Cloud)
OLLAMA_HOST=https://ollama.com
OLLAMA_MODEL=qwen3-coder:480b-cloud
OLLAMA_API_KEY=your_ollama_api_key_here

# Redis для кэширования
REDIS_URL=redis://redis:6379/0

# JWT для аутентификации
JWT_SECRET_KEY=your_super_secret_jwt_key_change_this_in_production_123
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Настройки парсера
PARSE_INTERVAL_HOURS=24
PLACE_EXPIRY_DAYS=30
SELENIUM_HEADLESS=True

# ID тестовых пользователей
MODERATOR_TG_ID=987654321
ADMIN_TG_ID=999999999